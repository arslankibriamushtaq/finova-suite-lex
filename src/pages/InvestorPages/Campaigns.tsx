import { Mail } from 'lucide-react';
// import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function Campaigns() {
  return (
    <PlaceholderPage
      title="Campaigns"
      description="Create and manage your email campaigns. Design beautiful newsletters, schedule sends, and track performance."
      icon={Mail}
    />
  );
}
