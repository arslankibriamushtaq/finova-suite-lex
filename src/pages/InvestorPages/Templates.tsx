import { FileText } from 'lucide-react';
// import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function Templates() {
  return (
    <PlaceholderPage
      title="Templates"
      description="Create and manage email templates. Build reusable designs for your newsletters and campaigns."
      icon={FileText}
    />
  );
}
