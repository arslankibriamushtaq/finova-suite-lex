import { Shield } from 'lucide-react';
// import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function SecurityCenter() {
  return (
    <PlaceholderPage
      title="Security Center"
      description="Monitor security events, manage access controls, and oversee cybersecurity measures across the platform."
      icon={Shield}
    />
  );
}
