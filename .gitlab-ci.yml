stages:
  - clone

# -----------------------------
# DEV PIPELINE
# -----------------------------
clone-dev:
  stage: clone
  tags:
    - new_mytm_dev
  variables:
    GIT_STRATEGY: none
  script:
    - cd /www/wwwroot/react-projects/financing-dashboard.xintdev.com
    - git pull origin feature/ui-migration
    - ./deploy.sh
    - echo "Code updated successfully"

  only:
    - feature/ui-migration
